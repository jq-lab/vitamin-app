# Vitora Swift Rebuild · 决策日志过滤器

> Batch: Product Reset
> 日期：2026-05-03
> 目的：在 canonical specs 和 Swift 实现计划前，记录本次产品 reset 的关键判断。

## 决策记录

| 决策 ID | 日期 | 决策 | 理由 | 证据 | 被拒绝的替代方案 | 后果 | 后续 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| D-001 | 2026-05-03 | 完全忽略 prior Claude-generated rebuild spec set | 用户明确指出 prior generated material 可能有旧截图和理解漂移 | 用户对话指令 | 复用 prior generated 结构或截图 | reset docs 从原始 `Spec/` 和 RN 证据重新开始 | 后续文档保持独立 |
| D-002 | 2026-05-03 | 原始 `Spec/` 作为意图、约束、候选池 | 原始 Spec 仍有稳定产品逻辑、合规、流程、候选功能 | `Spec/facts.md`, `Spec/userflows.md`, `Spec/compliance.md` | 全部丢弃旧文档 | 原始想法被过滤，不被照搬 | 后续 specs 引用过滤后的决策 |
| D-003 | 2026-05-03 | 当前 RN app 是证据，不是产品真相 | 用户已确认当前 app 混乱，不应复刻 | 源码扫描显示 mock 存储、Alert 占位、抽屉占位页 | 把 RN app 当唯一真相 | RN 用于说明存在过什么、哪里失败 | 模拟器只做定点查证 |
| D-004 | 2026-05-03 | 第一阶段目标是 P0 Swift MVP | 干净重建需要先收窄范围 | 用户在计划阶段选择 P0 Swift MVP | 追求原始 Spec 或 RN 全量 parity | inventory 中大量功能被标为 P1/P2/Out | 后续 specs 从 P0 切面开始 |
| D-005 | 2026-05-03 | 用决策表作为主要 reset 格式 | keep/redesign/defer/drop 必须可审计 | 用户选择决策表格式 | 只写叙述性文档 | `feature-inventory.md` 成为功能门 | 后续 docs 引用 inventory ID |
| D-006 | 2026-05-03 | 本 batch 不写实现规格 | 先去噪，再做技术计划 | 用户要求先写四个过滤器 | 现在开始 Swift 架构 | 本 batch 无 API、schema、Swift tasks | 技术 plan 作为后续 batch |
| D-007 | 2026-05-03 | 保留学习循环作为产品核心 | 这是 facts、flows、用户意图中最稳定的部分 | `Spec/facts.md` 核心循环、A/B、复盘 | 转成 tracker 或泛聊天 | P0 必须覆盖 morning state、A/B、record、review | 新 `facts.md` 应以此开头 |
| D-008 | 2026-05-03 | 保留 Today / Vitora / Cycle 三主区 | 心智模型稳定，原始 facts 要求三主区；D-032 后用户面对名称统一为 Vitora | `Spec/facts.md`, `Spec/ia.md`, RN tabs, D-032 | 增加第四个主 Tab | 支撑功能保持二级 | 新 IA 保留三主区 |
| D-009 | 2026-05-03 | P0 收缩抽屉/设置面 | 当前范围太宽，造成 placeholder 噪音 | RN drawer 有大量 later 页面 | 发布宽抽屉路由池 | P0 只暴露必要账户、隐私、数据、来源、提醒表面 | 新 IA 隐藏延期路由 |
| D-010 | 2026-05-03 | A/B 保留为核心，但必须诚实 | A/B 是学习动作，只有即时确认会破坏产品闭环 | `Spec/facts.md` 联动；EV-RN-002 确认当前选择方案后已有提醒时间雏形，但缺少 durable intent 和复盘承接 | A/B 只做装饰按钮 | P0 要么有真实意图与复盘路径，要么简化 UI | Swift 前先写 A/B userflow |
| D-011 | 2026-05-03 | Full energy ritual 进入 P0 Swift MVP | 用户确认它是产品第一印象和快速激活价值的一部分 | 原始 animation spec、RN EnergyOrb、用户决策 | 推迟到 P1 | P0 必须定义一个原生、可交付的最小仪式版本 | 后续视觉 batch 决定范围和验收 |
| D-012 | 2026-05-03 | Vitora 优先服务记录和理解，不优先做泛聊天 | Vitora 价值是上下文捕获和引导，不是泛聊天 | `Spec/pages/luna.md`, EV-RN-003 确认旧 Luna 记录入口存在但发送后缺少解析、确认和保存回流 | 做 chat-first assistant | Vitora surface 要突出上下文、校准、复盘和理解确认 | Vitora spec 定义 capture-confirm-save loop |
| D-013 | 2026-05-03 | Cycle 先做简单 | 当前 Cycle 混合了有用节律、日历和过多高级/商业表面；D-037 后日历归 Today 顶部 | `Spec/pages/cycle.md`, EV-RN-005, D-037 | MVP 发布完整展开页 | P0 保留阶段关系和能量动态，深度分析后移 | Cycle spec 从长期节律背景开始 |
| D-014 | 2026-05-03 | P0 不出现 placeholder 页面 | 占位页会让 app 未完成感很强，并放大范围 | RN drawer placeholder pages | 提前展示未来功能 | 延期功能不可见 | IA 必须定义可见 P0 nav |
| D-015 | 2026-05-03 | 隐私与合规是硬门 | 敏感健康数据需要先建立信任 | `Spec/compliance.md`, RN sanitizer 与 plain SQLite 对比 | 把隐私当后续实现细节 | 产品规格必须包含数据所有权和 LLM 最小化 | 新 compliance 与技术 batch 继续规格化 |
| D-016 | 2026-05-03 | Local LLM / Gemma 可延期实现，但必须提前感知 | 用户明确会在后续引入本地 LLM / agentic usage | RN 无相关实现；用户未来目标 | P0 直接实现本地 agent | P0 产品流保持模型无关，但数据、隐私、AI 规格要避免封死未来路径 | 产品 specs 后单独做 AI architecture batch |
| D-019 | 2026-05-03 | Swift 首发不必须 WeChat 登录 | 用户确认 Q-001 答案 | 用户决策 | 把 WeChat 作为 P0 account gate | FI-002 改为 drop；onboarding spec 后续定义非 WeChat 账户路径 | canonical onboarding spec 前决定具体登录/本地模式 |
| D-020 | 2026-05-03 | P0 具备 HealthKit 能力，但用户可跳过 | 用户确认 Q-002 答案 | 用户决策 | manual-only P0；或强制授权后才能使用 | App 必须在无 HealthKit 授权时继续工作，并有低数据体验 | Swift technical plan 定义 HealthKit 权限和数据降级 |
| D-021 | 2026-05-03 | P0 最小晚间复盘要对比当天干预前后效果 | 用户确认 Q-004 答案 | 用户决策，学习循环要求 | 只做泛总结或推迟复盘 | P0 复盘必须让用户感知 app 介入是否有用，用于快速激活 | userflows/spec batch 定义最小复盘内容 |
| D-022 | 2026-05-03 | 营养补给页面保留 | 用户确认 FI-039 | 用户决策，原始 Spec 候选功能 | 延期或移除该页面 | FI-039 改为 keep；P0 收口为设置/管理页 + Vitora 输入流快捷上下文 | 后续 spec 定义记录字段和保存确认 |
| D-023 | 2026-05-03 | Assistant 上滑 / 升级完整态进入 P0 | 用户确认 FI-026 keep for P0 MVP；D-035/D-036 已重新定义为 Vitora surface 与 contextual sheet upgrade | 用户决策，当前 RN 行为证据 | 删除完整 assistant 态或推迟到 P2 | FI-026 改为 keep / P0；正式目标是 Vitora 默认态、压缩态、输入态、语音态、context sheet 升级完整 Vitora | Vitora userflow/spec batch |
| D-024 | 2026-05-03 | Q-005 收口为 P0 轻抽屉，只显示 P0 必需项 | P0 需要隐私、数据、营养补给和提醒支撑，但不能复刻当前宽抽屉 | Simulator 抽屉检查、`src/app/drawer/index.tsx`、用户对 FI-039 / Q-005 的确认 | 删除抽屉；或展示当前宽抽屉和延期路由池 | facts 增加 F-P0-SUPPORT-002；IA 只保留六个支撑项，延期项不可见 | support / compliance / spec batch 继续细化支撑页 |
| D-025 | 2026-05-03 | 当前 app 必须作为新 specs 的实测 evidence / anti-evidence 输入 | 之前 IA/Userflows 主要依赖源码；用户要求真实点击、滑动和观察动效 | EV-RN-001 到 EV-RN-006：Today、A/B、旧 Luna、记录、抽屉、Cycle、日历和宿主导航污染均已用 Simulator / `iosef` 定点确认 | 只凭源码推断；或把旧截图当证据 | IA/Userflows/Inventory/spec 补充实测证据，但仍不把当前视觉作为 Swift 目标 | 后续每个行为不清的 batch 先用 Simulator 定点确认 |
| D-026 | 2026-05-03 | 视觉批次采用 source decision 分级 | Figma 节点有的仍可用、有的已经失效；当前 RN 有真实交互证据，也有明显噪音 | Figma MCP 读取节点；Simulator / `iosef` 点击、滑动和截图；`visual-evidence-map.md` | 盲目信 Figma；或盲目复刻 RN | `wireframes.md` 对每个 P0 界面标记 `figma-primary`、`hybrid`、`needs-redesign` 等判定 | `components.md`、`tasks-ux.md`、`plan.md` 必须引用 `EV-WF-XXX` 和 wireframe ID |
| D-027 | 2026-05-03 | 每个 P0 用户旅程步骤必须有前端最终参考 | 页面级 wireframe 还不等于旅程级实现索引；用户要求每一步都有最终前端参考 | `reference.md`、`wireframes.md`、`visual-evidence-map.md` | 只用页面截图；或只用流程图而没有前端参考 | 新增 `reference.md`，用 `RF-XXX-XX` 将 `UF-000` 到 `UF-010` 的每一步绑定到 `WF-XXX`、IA、证据或目标线框 | `components.md`、`tasks-ux.md`、`tasks.md` 的前端项必须引用 `RF-XXX-XX` |
| D-028 | 2026-05-03 | 组件和 design token 必须重建为 004 权威层 | Figma 文件没有可复用 variables/components；RN 有交互证据但样式散落且混入噪音 | Figma MCP `373:259`, `301:289`, `301:150`, `301:18`；Simulator Today/Luna/Cycle/Drawer 实测；原始 `Spec/components.md` | 直接复用 RN 样式；或等待实现阶段再临时抽组件 | 新增 `components.md` 和 `design-tokens.md`，组件引用 `C-*`，token 使用 `vt.*` | 后续 `tasks-ux.md`、`tasks.md`、Swift 实现必须引用组件 ID 和 token ID |
| D-029 | 2026-05-03 | 交互验收作为 Swift 实现前质量门 | 规格已覆盖 facts、flows、IA、wireframes、reference、components 和 tokens；进入实现前需要一套可执行的通过/失败标准 | Simulator / `iosef` 复查 Onboarding、Today、Today 记录、旧 Luna 首页、旧 Luna 沉浸态、轻抽屉、Cycle；`interaction-acceptance.md` | 直接从规格进入开发；或只验收视觉接近度 | 新增 `interaction-acceptance.md`，用 `IAC-*` 绑定流程、屏幕、组件、低数据、隐私和合规验收 | `tasks-ux.md`、`tasks.md`、自动化脚本和人工 QA 必须引用 `IAC-*` |
| D-030 | 2026-05-03 | 数据 / 领域模型成为技术方案前的领域权威 | P0 specs 已锁定流程、IA、交互验收和数据边界，但 Swift plan 前需要统一领域对象、状态机、事件和导出 / 删除 / AI 可用性 | `facts.md`、`spec.md`、`userflows.md`、`ia.md`、`data-privacy-ai-boundaries.md`、`interaction-acceptance.md`；RN store / service / migration 源码；Simulator 导出、账号移除、旧 Luna 记录检查 | 直接继承当前 RN migrations；或在 `plan.md` 才临时抽数据对象 | 新增 `data-model.md`，以 `DM-*`、`DE-*`、`DMN-*` 作为 plan / tasks 输入 | `plan.md`、`tasks.md`、`speckit.plan` 必须引用 `data-model.md` |
| D-031 | 2026-05-03 | 手写 SpecKit plan package 作为 `/speckit.tasks` 输入 | 004 specs 已有人工校准的 facts、UX、Figma/RN 证据、组件、交互验收和领域模型；直接让 `/speckit.plan` 重新解释会增加漂移风险 | SpecKit Pro 模板、本地 `.specify/templates/plan-template.md`、`tasks-template.md`、004 全量规格 | 现在直接运行 `/speckit.plan` 生成泛化 plan；或只写单个 `plan.md` | 新增 `plan.md`、`research.md`、`contracts/`、`quickstart.md`，把移动端内部服务契约作为 contracts | 下一批做 SpecKit Handoff；之后再运行 `/speckit.tasks`、`/speckit.analyze` 和分 phase implement |
| D-032 | 2026-05-05 | IA pivot 后，用户面对的 AI assistant 与 app 统一命名为 Vitora | 用户明确希望 app 名和 assistant 名统一，Vitora 是全局 Assistant Layer；旧 Luna 名称造成 Tab / 记录 / 聊天 / 建议心智混乱 | `wireframes-walkthrough-demo.md`, 用户 IA pivot 讨论 | 继续使用 Luna 作为产品角色名 | 正式 specs 的用户文案改为 Vitora；旧 Luna 只允许作为代码迁移债务或历史证据，不再作为产品 truth | facts、userflows、ia、spec、components、tasks 全部更新 |
| D-033 | 2026-05-05 | Today 首页收口为“现在状态 → 身体要素 → Vitora 今日建议” | 用户确认该心智最符合轻记录和 retention；独立记录区会让 app 变成打卡/记录工具 | `wireframes-walkthrough-demo.md` Today 最终 IA | 保留能量球 + AI监测今日 + 今日记录 + 建议入口四块并列 | Today 删除独立记录区；记录变成在状态/要素/建议中校准 Vitora | 更新 Today specs 和 UI tasks |
| D-034 | 2026-05-05 | Energy Ball 改为隐藏式仪式层，而不是首页常驻主 UI | 用户希望保留能量球，但不让它压住 Today 主结构；下拉揭示和晚间复盘更能体现仪式感 | `wireframes-walkthrough-demo.md` Energy Ball 动态 | 首页常驻大球；完全删除能量球 | 首页用曲线，Energy Ball 首次打开/下拉/晚间复盘出现 | 更新 wireframes、components、interaction acceptance |
| D-035 | 2026-05-05 | Vitora Tab 是 AI-native assistant surface，不是普通聊天页或功能 dashboard | 用户希望 Vitora 是活的 assistant 空间，有 Pixel Vitora、上下文、对话、快捷上下文和输入 dock | `wireframes-walkthrough-demo.md`, `design-language-demo.md`, `assets/design_04/vitora_tab.png` | 空白聊天页；功能卡片工作台；旧上滑沉浸聊天独立心智 | 默认态、滚动压缩态、输入态、语音态、context card、rich response、3/4 sheet upgrade 都进入正式规格 | 更新 userflows、ia、components、tasks |
| D-036 | 2026-05-05 | 非 CTA 唤醒 Vitora 先打开 3/4 contextual sheet，上滑再进入完整 Vitora | 这样能保持来源上下文，避免从 Today/Cycle 被硬切到另一个 Tab | `wireframes-walkthrough-demo.md` 全局唤醒规则 | 所有入口直接切到 Vitora Tab；到处放显性问 Vitora 按钮 | 新增 AskableSurface、context menu、callout、TipKit、VitoraContextualSheet | 更新 IA、交互验收和 tasks |
| D-037 | 2026-05-05 | Cycle 首页改为“当前周期阶段与今天 + 能量动态”，日历归 Today 顶部入口 | 用户坚持日历从 Today 左上进入；Cycle 应承载长期节律和能量动态 | `wireframes-walkthrough-demo.md`, `assets/design_04/cycle_d.png` | Cycle 首页放日历预览；继续旧基础概览+日历模型 | Cycle 第一层只放阶段关系卡与能量动态卡；二层提供透明解释和趋势探索 | 更新 facts、ia、wireframes、tasks |
| D-038 | 2026-05-05 | 设计语言锁定为“弥散渐变背景 + 清透拟态玻璃组件 + Pixel Vitora 陪伴体” | 用户拒绝 Apple Health dashboard 风格，选择更高级、AI-native、陪伴感强的视觉系统 | `design-language-demo.md`, `assets/design_04/*` | Apple Health dashboard；smooth orb；human avatar；pet mascot | 正式 tokens/components 必须加入 aura/glass/pixel/IP 状态和 low-lift CTA 规则 | 更新 design-tokens、components、interaction acceptance |
| D-039 | 2026-05-05 | `wireframes-walkthrough-demo.md` 与 `design-language-demo.md` 是本轮 pivot 的临时 high-rule evidence | 两个文档记录了正式 specs 之前的最终对齐决定；必须被拆入正式 specs，而不是被实现直接绕过 | 两个 demo markdown 文件 | 继续只实现旧正式 specs；或只读 demo docs 跳过正式 specs | 正式 specs 全量适配后，demo docs 保留为 evidence archive | 本 batch 执行正式规格系统更正 |
| D-040 | 2026-05-05 | 已完成 T001-T101 保留，但 T102 起必须先进入 IA / Design Pivot Adaptation | 当前 Swift 基础设施有价值，但 UI 到 US5 是旧规格产物；继续旧 US6 会扩大返工 | `tasks.md`, 已完成 Swift 文件与 UI 测试 | 从零重写；或继续旧 `/speckit.implement` | tasks 保留完成历史，T102+ 改为 pivot adaptation first | 更新 plan、tasks、quickstart、handoff |
| D-041 | 2026-05-16 | Today 首屏从下拉 Energy Ball 改为 Energy Bowl 主交互 + 实时预测 | 用户在模拟器观察后要求去掉下拉能量球，直接用能量碗承载状态、依据和实时预测，减少首屏提示和仪式层干扰 | 模拟器截图、用户对 Today 能量碗排版和实时预测的反馈 | 继续保留顶部下拉 Energy Ball；完全删除晚间能量对比 | Today 首屏移除下拉入口；能量碗可点开状态详情，并提供数据/依据入口；晚间复盘保留能量对比表达 | 更新 facts、spec、userflows、ia、wireframes、components、interaction acceptance 和 UI tests |
| D-042 | 2026-05-16 | Today 能量碗文字层和碗体分离，查看依据合并为查看数据/今日分析 | 用户在模拟器观察后指出能量碗、数字、标签和状态文案粘连，且查看依据应和睡眠/HRV/心率/周期数据合并展示 | 模拟器截图、用户对能量碗排版和身体要素横向展示的反馈 | 保留右下角悬浮查看依据；让碗体和文字继续叠在一起 | 碗体下移；监测标签上移并在行末提供 `查看数据`；点击进入 `今日分析`，横向展示身体要素并解释综合判断 | 更新 facts、wireframes、components、interaction acceptance、quickstart 和 UI tests |
| D-043 | 2026-05-16 | Vitora 输入改为全局 Dock，Tab 与输入职责分离 | 用户录屏显示语音态挤压成竖排，输入区混入周期小花和多余按钮，缺乏主次和节奏 | 用户录屏、输入栏截图、当前 Swift `VitoraInputDock` 证据 | 只修 Vitora Tab 局部输入；继续把 Tab 头部和输入放进同一大玻璃容器 | Today / Vitora / Cycle 底部使用同一 Vitora Dock 框架；Tab 只负责切换；输入区只保留语音/键盘、文字或内联语音条和发送。后续 D-046 收敛为仅 AI 管家页展开输入条，并移除输入内 `+`。 | 更新 facts、wireframes、components、interaction acceptance、tasks 和 UI tests |
| D-044 | 2026-05-16 | Today 首屏模式切换改为三项碗底光源托盘 | 用户指出数字不够突出，说明文字干扰，五项直线切换和能量碗不够和谐；心率/HRV 只需保留在查看数据依据里 | 用户截图、对能量碗数字和模式切换的反馈 | 继续五项直线分段切换；把心率/HRV 从今日分析完全删除 | 放大数字；移除数字上方 `今天` 和标签上方 `监测到`；首屏切换只显示综合/睡眠/周期，并用碗底光源托盘承接碗体 | 更新 facts、wireframes、components、interaction acceptance、tasks 和 UI tests |
| D-045 | 2026-05-17 | 全局 Dock 上排改为左侧 Tab + 右侧快捷记录，AI 管家触发展开输入 | 用户指出 Tab 切换应靠左，右侧需要独立记录快捷键，输入条只在 AI 管家页自动展开，不应被记录键触发 | 用户截图、对全局 Dock 信息架构和输入焦点的反馈 | 继续居中三 Tab；把记录混进输入胶囊；点击记录时切到 AI 管家并修改草稿 | Dock 上排左侧为 Today / AI管家 / Cycle，右侧为 `记录` 快捷键；记录打开 Vitora contextual sheet 且不切 Tab；点击 `AI管家` 才聚焦全局输入 | 更新 facts、wireframes、components、interaction acceptance、tasks 和 UI tests |
| D-046 | 2026-05-17 | 非 AI 页收起输入条，记录改为独立圆形 `+` | 用户明确纠正：Today / Cycle 只应显示 Tab 切换和记录快捷键，语音输入条只在 AI 管家展开；输入条内部不应再有 `+` | 用户截图、对上一轮实现的纠正反馈 | 继续在 Today / Cycle 显示输入条；保留输入内 `+`；右侧记录继续使用文字 pill | Today / Cycle 底部仅显示居中三 Tab 和右侧圆形 `+`；AI 管家页展开输入条；输入条只含语音/键盘、文本/语音条和强化发送按钮 | 更新 facts、wireframes、components、interaction acceptance、vitora-page spec、tasks 和 UI tests |
| D-047 | 2026-05-17 | Today 首屏收敛为综合能量碗 + 周期线轴 + 今日建议，完整实时预测移入今日分析 | 用户要求把睡眠/周期首页预测切换和首页实时预测图去掉，在碗下放周期线轴，并删除首页校准 chips；Dock 按图二改为三 Tab 同胶囊 + 右侧独立 `+` | 用户截图、对 Today 首屏与 Dock 样式的反馈 | 继续保留首页三模式切换、实时预测图和 `告诉 Vitora/睡得浅/压力大` chips；把睡眠/周期依据完全删除 | Today 首页固定综合能量；碗下展示周期线轴；`查看数据` 的今日分析展示综合实时预测和身体要素四卡；底部 Dock 三 Tab 同处居中玻璃胶囊，右侧独立圆形 `+` | 更新 facts、wireframes、components、interaction acceptance、quickstart、tasks 和 UI tests |
| D-048 | 2026-05-17 | Today 能量视觉、周期线轴和 Dock/Input 状态稳定性修正 | 用户指出能量数字有双层虚影、首页数据小标签应只进今日分析、周期条不应有背景波浪，且输入框和 title 栏反复串页 | 用户截图、对能量视觉、周期线轴、今日建议和 Dock/Input 的反馈 | 保留点阵双层数字；首页继续显示数据小标签；周期线轴继续玻璃背景/波浪；全局 Dock 继续用 zIndex 遮罩并在 sheet 下透出 | 能量数字改为单层高对比；首页只保留查看数据入口，数据小标签进今日分析；周期线轴改为无背景平滑曲线并按阶段着色；今日建议更行动化；全局 Dock 由 safe area 承载且 sheet 展示时隐藏，contextual sheet 使用稳定标题和本地输入 | 更新 facts、wireframes、components、interaction acceptance、quickstart、tasks 和 UI tests |
| D-049 | 2026-05-17 | Today 建议卡、能量碗和周期线轴进一步拟物收敛 | 用户指出身体翻译应放到今日建议后方且字体更大，建议应是 `吃+休息 / 运动+吃 / 休息+运动` 组合轮换；能量状态词应在 68 下方，碗形需更接近参考图，周期线轴应围绕碗底光晕成圆弧且文字全部在下端 | 用户截图、对建议卡、能量碗还原度和周期圆弧的反馈 | 继续解释型建议卡；`换一个` 打开 Vitora 浮层；周期仍为横向曲线并在上方显示气泡；状态词压在碗下 | Today 建议卡改为大字号身体翻译 + 两条组合推荐；`一件提醒` 进入现有提醒/分析路径，`换一换` icon 本地循环；周期线轴改为三阶段圆弧，仅展示前一段/当前段/下一段，全部文字放在下端；能量状态词直接跟随数字 | 更新 facts、wireframes、components、interaction acceptance、quickstart、tasks 和 UI tests |
| D-050 | 2026-05-17 | Today 圆弧反向上移，AI 管家页进入与上下文卡稳定 | 用户指出 Today 碗底圆弧方向理解反了，`查看数据` 应在状态词旁；AI 管家点击进入会直接大展开并串到不稳定状态，上拉应只展示完整聊天框；睡眠和营养卡应与周期卡结构一致 | 用户截图、AI 管家页交互反馈、当前 Swift `VitoraAssistantSurfaceView` 三态拖拽实现证据 | 继续中间上凸圆弧；`查看数据` 单独占一行；AI 管家进入默认 expanded，并用自由拖拽在三态间跳；周期使用专属报表卡而睡眠/营养用简短卡 | Today 状态词与 `查看数据` 同行；周期圆弧反向为碗底下沉弧并上移；AI 管家进入固定实体卡片态，上拉阈值进入聊天聚焦态且不切 Tab；周期/睡眠/营养统一为 `VitoraContextSummaryCard` | 更新 facts、wireframes、components、interaction acceptance、vitora-page spec、quickstart、tasks 和 UI tests |
| D-051 | 2026-05-17 | Today 水滴入碗动效与 Cycle 周期回顾首页升级 | 用户要求水滴从屏幕最顶端下落并进入能量碗，水位按能量状态积累；周期圆弧需上移到碗底白色光晕；Cycle 页面要按参考图完善为周期回顾卡片 | 用户截图、对 Today 动效和 Cycle 周期回顾卡的专业动效/视觉反馈、当前 `EnergyDropEmitter` 只做局部粒子和当前 Cycle 首页仅阶段/能量两卡的代码证据 | 继续局部星光粒子；让水位只做静态渐变；Cycle 继续旧阶段关系卡 + 能量动态卡首页 | Today 新增从屏幕顶部到碗口的水滴/涟漪/水位动效，Reduce Motion 静态水位兜底；周期三阶段圆弧嵌入碗底白色光晕；Cycle 首页升级为 `周期回顾`、等距花田、三件事总结、能量动态和三张洞察卡，仍不引入日历首页、打卡或任务心智 | 更新 facts、spec、wireframes、components、interaction acceptance、cycle-page spec、tasks、UI tests 和 Cycle change-log |
| D-052 | 2026-05-17 | 花园手册 MVP 收敛为轻闭环，Cycle 从花田地图改为 30 天成长册 | 用户提供 `Vitora_Garden_MVP_Document.md` 并标注 Today 周期圆弧应下移到红线区域，和碗保持约一个字空间；文档要求不新增 Tab、不做高成本花田地图，用花园手册 + 晚间复盘 + 每日成长卡片 + 周期成长册形成闭环 | 用户截图、`/Users/youxiang/Desktop/Vitora_Garden_MVP_Package 2/Vitora_Garden_MVP_Document.md`、当前 Cycle 花田地图实现证据 | 新增花园 Tab；保留花田地图下拉；在花园手册里直接种下；引入商城/VIP/露珠价格/枯萎/失败/红点 | Today 周期圆弧下移到碗外下方；Today 新增早晨成长反馈和花园手册 Sheet；AI 管家新增晚间复盘种子选择卡；Cycle 首页和详情改为 `30 天成长册`，不展示花田地图、打卡、连续天数或完成率 | 更新 facts、spec、wireframes、components、interaction acceptance、cycle-page spec/QA/change-log、vitora-page change-log、UI tests 和 Swift 实现 |
| D-053 | 2026-05-18 | 去除成长册、花园手册、种子选择和花园化晚间复盘包装 | 用户连续截图指出 `30 天成长册`、早晨成长反馈、花园手册入口和种子化晚间复盘卡全部需要去除 | 2026-05-18 用户截图与明确指令 | 继续保留四类花园/成长卡可见入口；把晚间复盘做成种植仪式 | Today 只保留状态主路径和 Vitora 今日建议；Cycle 只保留三件事总结、能量动态和节律洞察；Vitora 只在复盘可用时显示最小 `今晚复盘` 入口，不展示种子选择 | 更新 facts、spec、wireframes、components、interaction acceptance、cycle-page docs、vitora-page docs、UI tests 和 Swift 实现 |
| D-054 | 2026-05-18 | 全 App 背景升级为原生浅霜分层系统 | 用户提供干净高级的浅色参考图，并要求 1:1 拆解其干净原因后应用到 App 背景和子页面层次 | 2026-05-18 用户参考图与实现计划 | 直接使用参考图；继续使用旧水纹素材作为全屏底；每页单独写不同背景 | 新增 `PremiumAuraBackground`，Today / Vitora / Cycle / Sheet / Onboarding / Support 使用统一低饱和雾层、微纹理和白霜玻璃层级；旧 `WaterAuraReferenceBackground` 作为兼容 wrapper | 更新 facts、dynamic-aura-background docs、UI tests、截图验收 |
| D-055 | 2026-05-18 | 复盘种子实验层回归为建议反馈可视化 | 用户明确北极星指标是“Vitora 给出正确建议 → 用户愿意试 → 用户反馈有帮助 → Vitora 学得更准”，种子只用于把建议反馈变成可感知生长证据 | 2026-05-18 用户给出的睡眠种子玩法计划 | 完整花田地图、30 天成长册、商店货币、每日 AI 生图、连续天数、完成率或失败惩罚 | 允许晚间复盘反馈后选择 3 类睡眠种子；Today 建议卡显示昨晚种子状态和生理原因；AI 管家解释种子半开原因；Cycle 增加低权重 `本周期花架证据`，不作为首页主视觉或任务系统 | 更新 facts、spec、wireframes、components、interaction acceptance、cycle-page docs、vitora-page docs、UI tests 和 Swift 实现 |
| D-056 | 2026-05-18 | 全 App 色调改为 Warm Paper Aura 强层级 | 用户指出当前仍是死白，组件和背景同色；明确选择“暖米纸强对比 + 底色分区柔影”，要求重新定色调和设计规范 | 2026-05-18 用户参考图一/图二与 Warm Paper Aura 实施计划 | 继续用瓷白背景；只给能量碗局部加背景；用重橙照片或大面积蓝色渐变制造层级 | L0 改为暖米纸底，L1 主卡为珍珠白，L2 内嵌组件为暖灰白；`PremiumAuraBackground`、`PearlGradientSurface`、`GlassSurface` 统一使用暖米纸分区和短柔影 | 更新 facts、design tokens、dynamic-aura-background docs、Swift design system 和截图验收 |
| D-057 | 2026-05-19 | AI 管家 Tab 和输入快捷栏彻底分层，Tab 不再自动聚焦键盘 | 用户复盘两个录屏后指出 Tab 切换键和语音输入键心智混在一起，需要保持三 Tab 切换栏，同时把输入快捷键作为单独一栏；自动弹键盘会让“切换页面”和“开始输入”混淆 | 2026-05-19 用户录屏与底部 Tab/语音输入优化计划 | 点击 AI 管家后自动聚焦键盘；把语音、发送、记录或 `+` 混进 Tab 胶囊；新增第四个输入 Tab | `AI管家` Tab 只负责切换到 Assistant Surface；AI 页上层展示输入快捷栏，点击输入栏才聚焦；发送中显示 `Vitora 正在整理...` 状态轨；Today/Cycle 仍只显示三 Tab 和右侧独立 `+` | 更新 facts、wireframes、components、interaction acceptance、vitora-page change-log、Swift 实现和 UI tests |
| D-058 | 2026-05-19 | 真实花种作为花园轻证据层进入复盘种子闭环 | 用户提供 10 种真实花种、正向花语、分阶段低像素资产规则，并要求存入花园玩法素材，同时让今日建议、早安/晚安卡和复盘阶段实时展示种子变化 | 用户花种目录、2026-05-19 截图、当前 Swift 中 `SleepSeedCard` 与 `SeedPlantingSheet` 分散实现证据 | 恢复完整花园 Tab、花园手册、30 天成长册、花田地图、每日 AI 生图、让用户直接从 10 种花中选择 | 保留 `恢复 / 留余量 / 轻动` 三方向；由 Vitora 稳定分配真实花种；新增 `assets/garden-playbook/flower-catalog.md`；Today 建议卡、复盘阶段、早安/晚安卡和 Cycle 低权重花架证据读取同一张 `SleepSeedCard`；资产缺失时用 2D 像素 Canvas fallback | 更新 facts、spec、wireframes、components、interaction acceptance、tasks、Swift 模型/UI 和 UI tests |
| D-059 | 2026-05-19 | Today 建议卡改为开放态度进度和明确提醒组合 | 用户截图指出“专家建议组合”不应出现，顶部应直接显示开放态度进度；一键提醒要金黄色，提醒时间可上下滑动，提醒标签需明确到“时间 + 做什么”，组合标题改为 `休息 + 补剂` 且 `换一换` 放在组合旁 | 2026-05-19 用户三张截图与明确交互说明 | 继续显示大段身体翻译、`组合推荐` 胶囊、灰色一键提醒、只靠加减按钮调时间、模糊提醒标签或把换一换放到卡片底部 | Today 建议卡顶部改为 `开放态度 86 / 100` 分段进度和花种阶段；组合区显示 `休息 + 补剂` + `换一换`；行动标签明确时间与动作；提醒 Sheet 支持上下滑动时间和单选提醒 | 更新 facts、wireframes、components、interaction acceptance、quickstart、Swift 实现和 UI tests |
| D-060 | 2026-05-19 | Today 提醒卡改为像素早上太阳和嵌入式组合卡 | 用户要求今日卡片符号为低像素早上太阳；右侧/内部采用嵌入式卡片且背景更浓；`睡眠偏短` 和反馈来源要动态；去掉推荐行动上的勾选；数据来源和一键提醒保持一致；组合用左 + 右竖卡，中间加号；`白雏菊` 进度条在点击一键提醒后移到弹层顶部 | 2026-05-19 用户对今日卡片的详细设计要求 | 继续用 SF Symbol sparkle；行动列表后接勾选按钮；数据来源写“监测到”；动态来源和胶囊混在一起；提醒弹层没有顶部花种进度 | 标题改为 `今日提醒`，左侧低像素早上太阳；组合卡使用左右竖向行动卡 + `+`；动态指标胶囊显示 `睡眠偏短 / HRV / 黄体期`；行动卡不显示勾选；提醒 Sheet 顶部显示白雏菊进度 | 更新 facts、wireframes、components、interaction acceptance、quickstart、tasks、Swift 实现和 UI tests |
| D-060 | 2026-05-19 | 花园日常交互穿插与小雨点玩法补齐 | 用户要求明确花园设计如何穿插到 Today、今晚复盘、提醒 Sheet 和周期页，并逐一给出具体指标、种子入口、触发和选择方式 | 用户 2026-05-19 对今日建议、今晚复盘、周期玩法、小雨点和同气对比的补充说明；当前 `GardenReminderProgressRow`、`EveningReviewSheet`、`CycleSeedEvidenceShelf` 实现证据 | 恢复完整花园页面；把小雨点做成货币、连续天数、任务完成率或社交排行 | 新增 `assets/garden-playbook/daily-interaction-design.md`：Today 展示建议旁进度与花朵；提醒 Sheet 顶部展示一次进度；复盘反馈后展示三方向种子选择；Cycle 用 7-10 次花架证据和非排名同气对比形成强反馈；小雨点只作为照顾证据和置信度水分 | 更新花园玩法素材文档和决策日志 |
| D-061 | 2026-05-19 | Energy Bowl 与今日分析统一为同一解释入口 | 用户指出水位进场看不出从 0 到目标、周期线太细、能量数字与斜杠/查看数据缺少对角关系，并要求点碗和点能量都弹出同一个 `今日分析` | 2026-05-19 用户三张 Energy Bowl 参考图与逐条说明 | 继续让能量碗进入 `今日状态详情`、`查看数据` 进入另一套今日分析；水位只显示静态比例；周期圆弧所有段同宽；能量数字沿用普通字体且不随周期阶段变色 | 水位按分数映射并从 0 动画到目标，点击碗/数据入口触发短暂增长；当前周期圆弧段加粗着色；能量数字使用密集低像素格、黄体期金色和柔光；`今日分析` 顶部显示 `黄体期 Day 18`，用球形纹路报告统一睡眠、HRV、周期监测、解释、综合预测和今日推荐 | 更新 facts、spec、userflows、wireframes、components、interaction acceptance、tasks、Swift 实现和 UI tests |
| D-062 | 2026-05-19 | Today 建议组件收敛为智能监测长条管家方案 | 用户截图指出当前组件拥挤，`今日提醒` 和 `开放态度` 语义不准，睡眠胶囊晃眼，`一键提醒` 不应挤在卡片标题行，组合规划应集中时间和主题并尽量一行展示 | 2026-05-19 用户“这个组件重新优化一下”截图和逐条说明 | 继续使用 `今日提醒` 标题；把开放状态写成今日提醒状态；保留动态胶囊、`推荐组合`、重复 `休息+补剂` 标题、两张竖卡或圆形加号；随意截图不滚动到目标组件 | 标题改为 `智能监测`；开放状态归属身体翻译器；方案置于开放状态下方，用长条卡集中主题和早/中/晚推送时间；行动一行长条用大号 `+` 连接；`换一换` 和金黄色 `一键提醒` 放在卡片下方；验证截图时滚动到目标组件后再截 | 更新 facts、wireframes、components、interaction acceptance、quickstart、tasks、Swift 实现和 UI tests |
| D-063 | 2026-05-20 | Onboarding 改为注册进入页 + 聊天式补充信息页，完成后直接进入 Today | 用户指出注册页画风保持但内容要参考登录页；基础信息和设备绑定应放到第二页，由 Pixel Vitora 像聊天一样问一个、用户答一个；第三步直接进入 app | 2026-05-20 用户注册页参考图和明确流程说明；当前 Swift onboarding 已分散询问昵称、偏好和 HealthKit | 继续先问昵称；把运动偏好、关注方向和设备绑定拆成多页表单；保留 ready 说明页 | 第 1 页只处理 Apple / 微信 / QQ / 本地体验与协议，按钮必须可点；登录后跳过昵称；第 2 页合并关注方向、恢复方式、周期清晰度和可选设备绑定；`进入 Vitora` 后直接到 Today，HealthKit 仍可跳过 | 更新 facts、spec、userflows、ia、components、interaction acceptance、Swift 实现和 UI tests |
| D-064 | 2026-05-20 | Today 智能监测改为扑克牌式内嵌大卡 | 用户要求不移动 Today 上下关系和能量碗，只把下滑后的 `智能监测` 内部改成图一大圆角内嵌卡：顶部大标题占约 1.5/4，左侧今日推送两张竖卡，右侧周期建议一张长方形卡，首页不再展示花朵进度 | 2026-05-20 用户“智能监测卡片扑克牌式改版计划” | 继续在首页智能监测卡显示 `开放状态 86 / 100`、白雏菊进度条或完整花园实景；移动能量碗或让智能监测挤进首屏；恢复长条一行方案；增加新入口或任务心智 | `智能监测` 外层标题和像素早上太阳保留；内嵌大卡顶部显示 `身体翻译器` / `正在把今天翻译成管家方案`；左侧 `今日推送` 两张扑克牌式竖卡以小号 `+` 连接，右侧 `周期建议` 默认黄体期 D18 `午后留余量`；`换一换` 刷新两侧内容，金黄色 `一键提醒` 打开保留花朵进度的提醒 Sheet | 更新 facts、wireframes、components、interaction acceptance、quickstart、tasks、Swift 实现和 UI tests |
| D-065 | 2026-05-22 | Today 首页改为蓝晶 Pixel Egg、展开主题弧和 TodayInsightPanel | 用户提供图1半圆弧交互、图2当前问题、图3信息框架和图4蓝晶蛋参考，并要求点击小圆球展开主题；旧顶部三格切换和智能监测卡不再符合当前首页目标 | 2026-05-22 用户截图与“Today 首页交互与视觉改版计划”；当前 Swift `TodayStatusCard`、`TopModeSelector`、`VitoraDailySuggestionCard` 证据 | 继续保留顶部黑色三格切换；把 HRV/心率放在首页弧上；保留智能监测扑克牌卡作为首页下方主信息；把蛋改成 smooth orb 或普通图标 | Today 默认展示蓝晶混合 Pixel Egg、放大的 `68/100 查看分析` 和非交互节律摘要；点击蛋右侧小圆球展开 `今日能量 / 睡眠 / 经期 / 营养` 半圆主题弧；下方 `TodayInsightPanel` 按主题切换，今日能量默认用三环指标、黄色 Vitora 提示、原因/建议/提醒和 `+ 记一笔`；记录打开 contextual sheet，不切 Tab | 更新 facts、wireframes、components、interaction acceptance、tasks、Pixel IP change-log、Swift 实现和截图 QA |
| D-066 | 2026-05-22 | Today 圆圈展开和加号快捷记录语义拆分 | 用户指出当前右侧黑色 `X` 把圆圈展开和加号记录混在一起；圆圈应像参考图一样逐个展开信息条，加号应直接快捷记录；主数字应放在蛋下方，经期条按图3重做 | 2026-05-22 用户截图与“Today 圆圈展开与加号语义修正计划”；当前 Swift `TodayTopicOrbitArc`、`CycleSmileArc` 证据 | 用加号统一展开/收起圆圈；展开态显示 `X`；默认隐藏所有主题圆圈；把主数字继续放左下；周期条保留细线旧样式 | 四个主题圆圈默认围绕蛋显示且只显示图标；点击某个圆圈后该圆圈展开成信息条，点击其他圆圈时前一个收起，同一个圆圈再次点击收起；加号固定为快捷记录按钮并直接打开 contextual sheet；主数字移到蛋下方居中；经期条改为更粗柔和渐变、中心白圆 + 黄色阶段标记 | 更新 facts、wireframes、components、interaction acceptance、tasks、Swift 实现和截图 QA |
| D-067 | 2026-05-22 | Cycle 主回顾卡改为淡色卡片折页式 4 Tab | 用户要求参考物理纸张叠在主卡上方的折页式 tab，把 `本周 / 趋势 / 经历 / 监测` 作为四种看周期的心智模式；色彩需要帮助认知切换，但本轮选择淡色而非深色整卡 | 2026-05-22 用户“Cycle 折页式 Tab 主回顾卡改版计划”；当前 Swift `CycleReviewInsightCard` 三段 segmented tab 与 `cycle-page` 白玻璃规格证据 | 继续使用旧三段 `本周 / 趋势（月） / 周期` segmented tab；把 active/inactive 都做成灰色条；重排整个 Cycle 首页；用高饱和深色主体导致数据阅读疲劳 | 只替换 Cycle 顶部主回顾卡；四个淡色折页 tab 默认显示，active tab 与浅色主卡融合，inactive tabs 降饱和露出标签；点击 tab 让整张卡的色调和内容形态切换；保留下方能量动态、洞察卡、花架证据和全局 Dock；详情 sheet 继承当前 tab accent 并返回当前 tab | 更新 facts、cycle-page spec、wireframes、components、interaction acceptance、tasks、Swift 实现、UI tests 和截图 QA |
| D-068 | 2026-05-22 | 快捷记录页改为双入口、手动/AI 双模式和本地结构化解析 | 用户提供快捷记录 mockup，要求 Dock 右下角 `+` 与 Today 蛋旁统一圆弧 `+记录` 打开同一个记录页；选中的手动记录/AI记录卡片、sub-tab、选项圆卡和强度按钮颜色更深，并给出 AI 记录输入 `今天有点头痛吃了 B6` 的结构化解析目标 | 2026-05-22 用户“D-068 快捷记录页双入口优化计划”；当前 Swift `VitoraContextualSheet` 只有 `日常 / 经期` 简单 chip 结构，旧 `LunaRecordSheet` 仅作为历史证据；最后一个记账键盘参考不作为本轮布局依据 | 只调当前 chip 颜色；复用旧 Luna 用户文案；让 Today `+记录` 切换主 Tab 或改变主题展开；继承记账键盘的金额输入和财务分类；本轮接真实模型或新增时间线页面；新增持久化 schema | Dock `global.record.quick` 与 Today `today.unifiedOrbit.quickRecord` 打开同一个 `VitoraContextualSheet`，来源保留为 `快捷记录 / 今日能量记录 / 睡眠记录 / 经期记录 / 营养记录`；默认手动记录，支持 `症状 / 情绪 / 月经 / 营养 / 更多` sub-tab；AI 记录使用本地规则 parser 展示结构化确认；完成后关闭或轻反馈，不做时间线；不新增 schema、不接真实 AI | 更新 facts、wireframes、components、interaction acceptance、tasks、Swift 实现、unit/UI tests 和截图 QA |
| D-069 | 2026-05-22 | Today 首屏合并为统一圆弧轨道 | 用户指出现在右侧主题圆点、独立加号和下方经期条是分裂结构，希望按图2/图5把能量、睡眠、经期、营养、记录和经期能量条串成围绕蛋的一条圆弧系统 | 2026-05-22 用户最新截图与“Today 统一圆弧轨道改版计划”；当前 Swift `TodayTopicOrbitArc`、`CycleSmileArc` 和 `TodayView` 独立插入周期条证据 | 保留右侧一串圆点 + 下方独立周期条；让加号参与展开/收起；删除 Cycle 页长期节律模块；用 Playwright 验收原生 iOS | 只改 Today 首屏主视觉；新增统一轨道承载 `能量 / 睡眠 / 经期 / 营养 / +记录` 与底部彩色经期能量条；点击主题节点沿轨道切线展开深色信息胶囊；`+记录` 只打开 contextual sheet；去掉 Today 独立 `CycleSmileArc` section；Cycle 页 D-067 内容不删除 | 更新 facts、wireframes、components、interaction acceptance、tasks、Swift 实现、UI tests 和截图 QA |
| D-070 | 2026-05-22 | Today 统一轨道修正为右侧单半圆弧和浅白弧形展开条 | 用户指出当前能量选中态仍是斜着的直线胶囊，目标是右侧只有一条半圆弧轨道，所有按钮贴同一条弧排列；选中态应让轨道局部变粗为淡色渐变白弧形信息条，不能是水平胶囊或斜向圆角矩形；蛋下方独立周期条要并入右侧弧语义 | 2026-05-22 用户两张参考图与“D-070 Today 右侧单半圆弧轨道修正计划”；当前 Swift `TodayUnifiedOrbitTopicButton` 用 Capsule 画展开态，`TodayUnifiedOrbitTrack` 仍单独绘制底部周期条 | 继续用旋转 Capsule；保留蛋下方 `排卵期 / 黄体期 D18 / 月经期` 周期条；节点旁显示文字标签；让 `+记录` 参与展开/收起 | 五个节点沿同一右侧椭圆半圆路径贴近排列，默认只显示白色圆形图标；选中主题由 Canvas 沿同一弧线绘制局部加粗浅白渐变弧段，icon 嵌入弧段起点，数值沿切线方向展示；`+记录` 只打开 contextual sheet；蛋下方不再展示独立周期条 | 更新 facts、wireframes、components、interaction acceptance、tasks、Swift 实现、UI tests 和截图 QA |
| D-072 | 2026-05-24 | Today 顶部日期能量卡压缩，周期日历改为文具浮层 | 用户要求左上角日历/能量入口按图三压缩到最小，不显示睡眠和步数；点击后周期日历需参考提供的 Swift mock 和高保真图，形成左侧 3/4 宽纸质浮层、右侧虚化主页背景、浅粉便签和木制记忆箱 | 2026-05-24 用户 D-072 计划、Swift mock、参考图和当前 Swift `TodayHeaderDailyEnergyCard`、`TodayCalendarSheet` 证据 | 保留旧“周期日历”大标题和“今天在周期时间线上的位置”副标题；入口继续显示睡眠/步数/长日历 icon；把日历放回 Cycle 首页；继续使用普通信息卡、行动链、过去几天和普通彩色 memory box | Today 左上角入口只显示星期、日期、`黄体 D18`、横向能量条和数值；周期日历保持 Today 二层入口，左侧约 3/4 宽文具浮层，顶部极简返回/月切换/珍珠更多，内容为阶段图例、英文周标题月历、撕边便签和 2x2 木制记忆箱 | 更新 facts、wireframes、components、interaction acceptance、tasks、Swift 实现、截图 QA |
| D-073 | 2026-05-24 | Cycle 主回顾卡改为三页健康报告卡，并生成 1536×1024 三卡展示图 | 用户要求一张 1536×1024 极简健康数据报告展示图，同时把 Cycle 页面里的卡片替换为同一套报告语言；手机内不应硬塞三张并排卡，应使用图二的叠层单卡文件夹 tab | 2026-05-24 用户 “D-073 Cycle 报告卡与 1536 展示图计划”；当前 Swift `CycleReviewInsightCard` 仍是 D-067 四 tab，规格和 UI tests 仍包含 `经历 / 监测` | 继续保留四 tab `本周 / 趋势 / 经历 / 监测`；在 iPhone 内并排塞三张报告卡；恢复 Cycle 日历或下方能量动态、节律洞察和花架证据 | Cycle 首页主卡收口为 `本周 / 趋势（对比） / 近期` 三个文件夹 tab，默认本周；三页分别展示本周柱状复盘、趋势月度对比、近期能量折线；首页不恢复日历、能量动态、节律洞察或花架证据；展示图用 HTML/CSS/SVG 输出 `1536×1024` PNG，不进 app bundle | 更新 facts、wireframes、components、interaction acceptance、cycle-page spec、tasks、Swift 实现、UI tests 和 PNG 截图 QA |
| D-074 | 2026-05-25 | 快捷记录误触修复、首页花组件与日历入口压缩 | 用户指出快捷记录无法退出、完成不可点、关闭时会误触后方日历；同时要求删除图4数字记录、首页蛋替换为图1花朵能量组件，左上角日历入口改为图3小日历 icon | 2026-05-25 用户 D-074 计划、快捷记录/首页截图；当前 Swift `VitoraContextualSheet` 有金额键盘且完成依赖金额，`AppRouter` 无 hit-test backdrop，`EggCompoundView` 使用 PixelFrostedGlassEgg，`TodayHeaderDailyEnergyCard` 仍占用大卡片区域 | 继续保留金额输入和数字键盘；关闭按钮缺失；让透明区域点击透传到 Today 日历；保留首页主蛋；让整块 topContext 都可打开日历 | `VitoraContextualSheet` 增加 44pt 关闭按钮、下拉关闭和全屏透明 backdrop；手动模式删除金额输入/数字键盘，只保留分类、字段和图片/拍照/语音入口，默认分类即可完成；首页主视觉替换为 SwiftUI/Canvas 花朵能量组件并保留 `egg.compound.mascot` id；左上角入口缩为 38-44pt 小日历 icon，抽屉开关动画期间禁止后方 hit testing | 更新 facts、wireframes、components、interaction acceptance、tasks、Swift 实现、构建和截图 QA |
| D-075 | 2026-05-25 | Today 记录反馈、首屏信息层级压缩与快捷记录健康大纲 | 用户要求快捷记录完成后回到 Today 时聊天/反馈条展示记录摘要和温馨提示；花组件、右侧胶囊切换和 68 分不应继续占用过高首屏，也不能让红色虚线挡到聊天框；随后明确要求手动记录不再使用 `支出 / 收入 / 转账`，改为 `是经期 / 否经期` 与健康大纲，默认展示 5 个高频选项并可展开更多 | 2026-05-25 用户 D-075/D-076 计划、Today 截图和快捷记录大纲截图；当前 Swift `VitoraContextualSheet` 完成后只关闭，Today `VitoraChatBubble` 只展示当前维度静态提示，`EggCompoundView` 视觉区偏高，手动页仍残留财务分类心智 | 把记录反馈做成持久 schema 或时间线；继续只 toast 已保存；让花组件继续占半屏；在 TodayInsightPanel 内混入最近记录；继续保留财务分类、金额键盘或新增购买/诊断建议 | `VitoraContextualSheet` 在手动和 AI 内容下方新增留言条；手动页改为 `是经期 / 否经期` + 健康大纲，默认 5 个词条、点击大纲或更多展开支线，删除财务分类/金额心智；完成后只写入运行态 `RecentRecordFeedback` 并关闭；Today 首屏反馈条上移，优先显示记录摘要和生活方式温馨提示，花组件压缩到上方约 1/3 且红色虚线避开反馈区；`TodayInsightPanel` 下移为次级分析卡 | 更新 facts、wireframes、components、interaction acceptance、tasks、Swift 实现和截图 QA |
| D-076 | 2026-05-25 | Cycle 顶部增加原生 SwiftUI 花之地图 | 用户希望在周期页面顶部增加“花之地图”模块，参考专注森林自然系地图，但明确不能用 RN/Expo、图片、地图 SVG、手机壳或额外背景，当前周期报告卡应下移保留 | 2026-05-25 用户 D-075/D-076 计划与花之地图参考图；当前 Swift `CycleView` 仅展示 header + D-073 报告卡，facts 仍禁止花田地图 | 恢复完整花园 Tab、成长册、打卡/完成率、RN 地图或图片资源；把 Cycle 改回日历首页；删除 D-073 报告卡 | 在 Cycle 顶部新增 `FlowerMapView`，使用二维字符串数组拼出不规则菱形深圳轮廓；mock 深圳 24/36 下一站广州；支持种花、空地点击、花详情、玩法说明和城市节点切换；报告卡下移继续保留三页文件夹报告；不新增持久化 schema、不引入任务/打卡心智 | 更新 facts、wireframes、components、interaction acceptance、tasks、Swift 实现、构建和截图 QA |
| D-077 | 2026-05-25 | Today 花朵首页状态表达与 Aura 背景加浓 | 用户提供花朵首页图和 ZIP 花朵玩法文档，要求 Today 背景左上粉橘、右下蓝绿更浓，底部不要灰，花朵主视觉要按 80% 成长和同品种共生表达，同时不能挡到聊天反馈层 | 2026-05-25 用户 D-077 计划、花朵参考图、ZIP 文档 `01_玩法规则.md` / `03_状态机与触发逻辑.md`；当前 Swift `PremiumAuraBackground` 底部灰感明显，`FlowerEnergyBloomView` 只有前天/昨天节点和简化半碗 | 引入 ZIP 图片资产或完整花园系统；恢复收割、花粉、图鉴、种子兑换、连续打卡；只调背景色不改花朵状态语义；让右侧胶囊/虚线继续遮挡反馈条 | Today 背景增强为顶部清晰、中部凝态玻璃、底部珍珠白 + 蓝绿霜态玻璃；花朵组件展示前天/昨天/今天三节点、80% 能量碗、`黄体期`、`达到 80% 时将成长为向日葵` 和 `100% 同品种双朵共生` 虚线气泡；点击花只做轻微生长/发光反馈；不新增任务化花园入口 | 更新 facts、wireframes、components、interaction acceptance、tasks、Swift 实现、构建和截图 QA |
| D-078 | 2026-05-25 | Today 花朵主视觉阈值与动效修正 | 用户指出 D-077 落地后花、气泡、今天节点、80% 和水桶堆叠，且当前 68% 被错误画成 80% 开花态；要求 68% 只进入水桶，80% 是成长阈值，未达阈值应显示种子苗，并去掉呼吸缩放 | 2026-05-25 用户 D-078 计划和三张参考图；当前 Swift `FlowerEnergyBloomView` 固定 `growthProgress = 0.80`、默认画成熟花和共生气泡、整体 `scaleEffect` 呼吸 | 继续把 80% 当当前值；默认展示成熟向日葵或共生气泡；保留曲线时间线和整体忽大忽小呼吸；让提示、节点、读数继续堆叠 | `FlowerEnergyBloomView` 改为 `energyProgress = 0.68` 数据驱动；时间线改直线，左绿虚线、中间橙色水桶上沿、右粉虚线；68% 时今天节点显示种子苗和阈值提示，碗内显示 `68%` 与 `黄体期`；达到 80% 才开花，100% 且同品种才显示共生气泡；删除整体呼吸缩放，只保留 5.5-7s ±1.5° 轻摆、水面低幅波纹和点击一次性反馈 | 更新 facts、wireframes、components、interaction acceptance、tasks、Swift 实现、构建和截图 QA |
| D-079 | 2026-05-26 | Today 移除花下独立 `68/100 查看分析` | 用户截图明确要求删除花组件下方独立大号分数和 `查看分析`；D-078 后能量已在水桶内表达，独立分数重复且占用聊天反馈区上方空间 | 2026-05-26 用户截图；当前 Swift `EggCompoundView` 中独立 score/CTA 区块 | 保留花下大号分数和 CTA；让 `68%` 同时出现在水桶和花下；点击花下 CTA 继续进入今日分析 | 移除 `EggCompoundView` 的独立 score/CTA，仅保留水桶内 `68% / 黄体期`；花组件点击只做轻反馈；今日分析入口由下方 `TodayInsightPanel` 标题 / `为什么` 等承接 | 更新 specs、Swift、构建与截图 QA |
| D-080 | 2026-05-26 | Today 花朵主视觉宽画布与挤压修正 | 用户指出当前图 1 与目标图 2 差距大，提示、种子苗、今天节点、水桶读数和右侧“今日”胶囊被挤在一起；要求按图 2 的横向比例重排，但默认进度仍为 68% 种子苗 | 2026-05-26 用户 D-080 计划和对比截图；当前 Swift `EggCompoundView` 使用窄花朵画布并把 `ScrollableChipDial` 叠在水桶右侧 | 继续在 292x176 左右小画布里堆叠；把图 2 的 80% 当默认首页状态；让右侧胶囊覆盖水桶、`68%`、节点或红色虚线 | 花朵画布扩为接近整屏宽 hero band，时间线、水桶、种子苗、阈值提示和读数按横向层级重排；右侧维度胶囊改为独立悬浮层，不覆盖水桶和读数；默认仍按 `68%` 展示种子苗，80%/100% 仅用 mock 验证 | 更新 specs、Swift、构建与截图 QA |
| D-081 | 2026-05-26 | Today 花朵主视觉疏散排版微调 | 用户指出 D-080 后图 1 仍较拥挤，图 2 的排版更舒展；要求位置不够时稍微疏散并调整位置 | 2026-05-26 用户对比截图；当前水桶左沿仍贴近昨天节点，阈值提示、种子苗、今天节点、读数和右侧胶囊视觉距离偏近 | 继续仅放大画布但不重算节点/水桶比例；为了首屏高度压缩而让元素继续堆叠 | 收敛水桶宽度，拉开前天/昨天/今天节点，阈值提示上移，右侧胶囊缩小并上移，使图 2 的留白和层级更接近；默认仍为 68% 种子苗 | 更新 specs、Swift、构建截图 |
| D-082 | 2026-05-26 | Today 反馈条移除后置记录/提醒圆按钮 | 用户截图指出 Vitora 反馈条右侧灰色圆形 `记录饮食 / 补水提醒` 入口需要全部删除；这类入口挤占聊天反馈层，也和 D-074/D-075 的统一快捷记录入口重复 | 2026-05-26 用户截图；当前 Swift `TodayRecordFeedbackCard` / `VitoraChatBubble` 仍渲染右侧 `hints`，`DimensionType.actionHints` 仍保留 `记录饮食 / 补水提醒` 等文案 | 继续在反馈条右侧追加记录/提醒按钮；把这些按钮改成更淡但保留；让它们承担快捷记录入口 | 删除反馈条右侧 `hints` 渲染和主题 actionHints 文案；Today 反馈条只保留头像、Vitora、消息和合规说明；记录入口只保留全局 `+`、快捷记录 sheet 和明确的面板内入口 | 更新 specs、Swift、构建截图 |
| D-083 | 2026-05-26 | Today 折叠态去除维度 chips，compact 小蛋改为微缩花盆 | 用户截图指出折叠态不需要展示 `今日 / 睡眠 / 经期 / 营养` 这些卡片；图一顶部小蛋应换成花盆，表达当前能量状态的微观缩小图 | 2026-05-26 用户截图；当前折叠态仍可能复用维度 chips 或 `PixelEggView` compact header | 继续在折叠/聊天态显示维度 chips；继续复用 Pixel Egg 和 activeDimension 分数；让睡眠/经期/营养在折叠头部占位 | 折叠/聊天态只保留左上小日历和右上微缩花盆 + 当前能量 `68/100`；维度切换只在展开主视觉右侧面板中存在；旧折叠头组件同步改为花盆语义 | 更新 specs、Swift、构建截图 |
| D-084 | 2026-05-27 | Cycle 顶部胶囊改为报告维度并删除下方文件夹 tab | 用户给出参考图，要求地图下面只框住一块具体内容做详细介绍；删除下方 `本周 / 趋势（对比） / 近期` 文件夹切换，并把顶部 `当前城市 / 路线 / 地球` 改为 `本周 / 趋势对比 / 近期` | 2026-05-27 用户截图和明确三条要求；当前 Swift `CycleReviewInsightCard` 仍在地图下方渲染报告 tab，Header 胶囊仍承担地图 segment 语义 | 保留两套切换；继续让 Header 控制城市 / 路线 / 地球；让地图下方同时出现文件夹 tab 和详情框 | Header 胶囊成为报告维度切换；花田地图只由深圳 / 广州节点和点状路线承担城市预览；地图下方只保留单张详情报告框，内容随 Header 的本周 / 趋势对比 / 近期切换 | 更新 facts、cycle-page spec、wireframes、components、interaction acceptance、Swift 实现、UI tests、构建和截图 QA |
| D-085 | 2026-05-27 | Dock `+` 快捷记录增加长按语音和 AI 内打字/语音切换 | 用户提供 D-081 计划，要求保留 `+` 点按打开快捷记录手动页，同时长按直接进入语音输入；打字输入只放在快捷记录 sheet 内，不恢复首页常驻输入框、发送按钮或 Tab 栏大背景 | 2026-05-27 用户 D-081 计划、前序 D-074/D-075 快捷记录结构和当前 Swift `GlobalVitoraDock` / `VitoraContextualSheet` 证据；D-081 已被 Today 花朵排版使用，因此本条顺延为 D-085 | 把 `+` 改成只做语音；恢复首页常驻输入栏；新增第四 Tab 或独立语音页；接真实模型或记录语音/转写原文到日志；权限拒绝时崩溃 | `global.record.quick` 点按仍进入手动健康大纲；长按约 0.42s 触发 haptic 和小型 `语音记录` 浮岛后打开同一 `VitoraContextualSheet` 的 `AI记录 > 语音`；辅助操作提供 `语音记录 / 打字记录`；AI 记录内新增 `语音 / 打字` 二级切换，语音不可用时降级打字，解析继续用本地 parser | 更新 facts、wireframes、components、interaction acceptance、tasks、Swift 实现、UI tests、构建 QA |
| D-086 | 2026-05-27 | Today 折叠态改为全屏前景聊天卡 | 用户要求今日折叠态不要新增 Today 背景，只保留当前 Aura 背景；前景折叠页要占据整个屏幕；删除时间轴和时间胶囊，改成图 3 风格留言头像 + 聊天气泡；后续明确要求去掉前景卡后方的大磨砂玻璃底板 | 2026-05-27 用户参考图和三条明确要求；当前 Swift `TodayInlineChatPlaceholder` 仍有额外外层底板、整体偏小、左侧虚线时间轴和时间胶囊 | 继续只铺裸聊天气泡；恢复首页常驻输入框；保留额外灰白页面底板或大磨砂玻璃底板；把折叠态做成任务列表或独立记录区；引入图片素材复刻参考图 | 折叠态旧顶部小日历/微缩花盆在完全折叠时淡出，`Today` 标题和前景聊天卡直接浮在现有 Aura 背景上；聊天框顶部保留轻量原生像素时间/深圳/周期条；消息区删除时间轴和时间胶囊，Vitora 消息使用柔白 Pixel 头像 + 气泡，用户消息右对齐；不恢复任务清单、常驻输入框、额外 Tab 或外层磨砂底板 | 更新 facts、wireframes、components、interaction acceptance、Swift 实现、构建截图 QA |
| D-087 | 2026-05-27 | 底部 Tab 栏改为一体式凹槽 Dock | 用户提供参考图并要求当前 Today/Cycle 胶囊 + 右侧独立 `+` 改为白色圆角 Dock，中间挖出凹槽承载悬浮 `+`，同时保留 D-085 的点按手动记录和长按语音记录 | 2026-05-27 用户 D-087 计划、参考图和当前 Swift `GlobalVitoraDock` 证据 | 继续保留居中玻璃胶囊 + 右侧独立 `+`；恢复底部常驻输入框或发送按钮；把 `+` 改成第三个 Tab；新增第四 Tab 或改变快捷记录 sheet 内容 | `GlobalVitoraDock` 改为一体式白色/珍珠白凹槽 Dock：左右为 `今日 / 周期`，中心凹槽悬浮蓝紫色圆形 `+`；保留 `primary.tabbar`、`tab.today`、`tab.cycle`、`global.record.quick`，点按 `+` 打开手动快捷记录，长按进入 AI 语音记录；sheet 打开时 Dock 隐藏，底部不再出现大面积发雾背景 | 更新 facts、wireframes、components、interaction acceptance、tasks、Swift 实现、构建和模拟器 QA |
| D-088 | 2026-05-27 | 底部凹槽 Dock 下移镶嵌，中心 `+` 改为蓝绿色霜态玻璃 | 用户指出当前 Dock 仍浮得偏高，中间 `+` 蓝紫色过实，要求整个 Tab 栏下移镶到底部，并把中心 LOGO 区换成更透明的蓝绿色霜态玻璃质感 | 2026-05-27 用户两张 Dock 截图和明确微调要求；当前 Swift `GlobalVitoraDock` 的 `+` 使用高饱和蓝紫 RadialGradient，外层仍有明显悬浮间距 | 继续把 Dock 悬在底部上方；保持蓝紫实心球；恢复底部大面积输入框背景或发送按钮 | `GlobalVitoraDock` 整体下压贴近 bottom safe area，增加轻霜态嵌入底座；中心 `+` 使用青蓝绿色半透明材质、白色高光与水感阴影，保留点击手动和长按语音记录语义 | 更新 specs、Swift 实现、构建和截图 QA |
| D-090 | 2026-05-27 | 底部 Dock 尺寸收窄并强化中心 `+` 短按/长按入口 | 用户确认当前状态不错，但要求 Tab 栏上下左右再缩窄，`今日 / 周期` 不需要这么宽；中心 `+` 要短按手动记录、长按语音输入，并且颜色更透明、更霜态玻璃 | 2026-05-27 用户 Dock 截图和明确两点要求；当前 D-088 Dock 已有凹槽和青蓝 `+`，但本体仍偏宽、左右 Tab 胶囊偏大，中心按钮颜色仍略实 | 继续放大 Dock；把短按/长按拆成多个常驻入口；恢复底部输入框、发送按钮或右侧独立 `+` | 收窄 `GlobalVitoraDock` 宽高、左右 Tab 胶囊和中心 `+` 直径；降低中心 `+` 青蓝渐变不透明度，保留 `global.record.quick` 短按 `.manual`、长按 `.voice` 和无障碍 `语音记录 / 打字记录` | 更新 specs、Swift 实现、构建和截图 QA |
| D-089 | 2026-05-27 | Today 改为低代码标题 + 创意趋势卡 | 用户要求 `Today` 按参考图做低代码/像素文字，折叠态顶部信息压缩成细进度条；展开态去掉粉色日历小卡和花朵玩法主视觉，先换成图 6 风格小趋势卡并保留底部露出 | 2026-05-27 用户图片 1-7 与实施计划；当前 Swift 折叠态标题仍是普通系统字，展开态仍引用 `EggCompoundView` 花朵玩法 | 继续展示粉色日历小卡；继续展示水桶/种子苗/花朵成长玩法；把图 6 做成图片资产；恢复任务清单、打卡、完成率或成长册 | 折叠态使用原生像素 `Today` 标题、细线进度条和创意日温馨留言；展开态顶部用同款日期 + `Today` 信息打开左侧日历抽屉；首屏主视觉为原生 SwiftUI `TodayCreativeTrendCard`，含浅色圆角卡、点阵背景、粉色趋势曲线、68/100、黄体期 D18 和深圳；底部 Dock 不改 | 更新 facts、wireframes、components、interaction acceptance、Swift 实现、构建和截图 QA |
| D-091 | 2026-05-27 | Today 展开/折叠复用同一聊天记录卡 | 用户指出展开态仍出现旧 `今日能量` 环形指标卡，要求去掉，并确保展开状态和缩起状态的聊天信息记录一致且能展示能量进度；折叠态底部空隙需要收掉 | 2026-05-27 用户图一旧卡和图二底部空隙截图；当前 Swift 展开态仍渲染 `TodayInsightPanel`，折叠态白卡高度不足 | 继续展示三环 `今日能量` 卡；展开态和折叠态使用两套不同聊天文案；折叠态白卡底部留空 | 展开态移除 `TodayInsightPanel` 环形卡，改用与折叠态相同的 `TodayJournalChatCard`，同一套创意日留言、Vitora 回复和细线能量进度；折叠态白卡最小高度加长到 Dock 后方，减少底部空隙 | 更新 facts、wireframes、components、interaction acceptance、Swift 实现、构建截图 QA |
| D-092 | 2026-05-27 | Today 展开态聊天区去背景并只保留留言 | 用户指出展开态不需要显示顶部 `时间 / 深圳 / 黄体期 D18 / 68` 信息条和创意日说明块，并要求去掉聊天区白色背景，只保留留言信息 | 2026-05-27 用户两张展开态截图；D-091 后展开态虽然移除了旧三环卡，但仍复用折叠态完整白卡和顶部能量进度 | 继续在展开态显示折叠态信息条、进度条、创意日说明和白色卡背景；把展开态做成又一张完整卡片 | 展开态 `TodayJournalChatCard` 使用同一留言数据但关闭 context header、warm note 和 card chrome，只露出 Vitora/用户留言气泡；折叠态仍保留完整白卡、细线能量进度和底部填充 | 更新 specs、Swift 实现、构建截图 QA |
| D-017 | 2026-05-03 | 不复制当前 RN 视觉 | 用户已否定当前 app 质量，视觉需要 reset | RN 源码和模拟器证据 | 逐像素复刻当前 app | 未来视觉真实来源是 Figma + iOS 判断 | 视觉 batch 必须读取 Figma |
| D-018 | 2026-05-03 | 合规 copy 集中管理，本 batch 不复制全文 | reset filters 不应制造过期法律文本 | `Spec/compliance.md` 是当前来源 | 把所有 copy 粘进 reset docs | reset docs 只引用合规来源和约束 | 新 canonical compliance spec 再复制批准文案 |

## 已解决产品问题

| 问题 ID | 答案 | 后果 |
| --- | --- | --- |
| Q-001 | Swift 首发不必须 WeChat 登录 | FI-002 drop；onboarding 后续定义非 WeChat 账户路径 |
| Q-002 | P0 具备 HealthKit 能力，但对用户可选 | App 必须支持未授权/无设备的手动和低数据模式 |
| Q-003 | Full energy ritual 进入 P0 | 视觉 batch 要定义最小可交付仪式版本 |
| Q-004 | P0 最小晚间复盘是“对比 app 当天干预前后的效果” | userflows/spec batch 必须把复盘做成价值激活点，而不只是总结 |
| FI-026 | Vitora contextual sheet / full assistant upgrade 保留在 P0 | Vitora specs 必须覆盖默认态、压缩态、输入态、语音态、rich response、3/4 sheet 升级完整 Vitora |
| Q-005 | P0 保留轻抽屉，只显示 P0 必需支撑项 | facts 与 IA 已收口；宽抽屉、延期项、占位项不进入 P0 可见导航 |

## 开放产品问题

当前无阻塞 Product Reset 和 IA 的开放问题。后续若出现新的 P0 入口争议，先补充本日志，再进入实现规格。

## 决策使用规则

后续 specs 必须至少引用以下之一：

- 本文件中的 Product Reset decision ID
- `feature-inventory.md` 中的 feature inventory ID
- 其他 reset docs 中的产品或 UX 原则

无法追溯到这些来源的功能，不进入 Swift rebuild 范围。
