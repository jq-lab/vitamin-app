# Vitora Swift Rebuild · 交互验收规格

> 规格集：`004-vitora-swift-rebuild`
> 批次：IA / Design Pivot Adaptation
> 日期：2026-05-05
> 状态：当前生效

本文档定义 P0 交互、状态、视觉语言和 QA 门禁。它已废弃旧 Luna 沉浸聊天唯一心智、旧 Cycle 日历首页、旧 Today 独立记录入口和 Apple Health dashboard 视觉方向。

## 0. 权威规则

| Rule ID | Rule |
| --- | --- |
| IAC-AUTH-001 | 若实现与 `facts.md`、`spec.md`、`ia.md`、`wireframes.md` 冲突，交互验收不通过。 |
| IAC-AUTH-002 | `wireframes-walkthrough-demo.md` 和 `design-language-demo.md` 是 pivot evidence；正式验收以本文和对应 formal specs 为准。 |
| IAC-AUTH-003 | 每个 `/speckit.implement` phase 结束必须按本文跑 simulator / iosef / XCUITest / 人工截图 QA。 |
| IAC-AUTH-004 | 旧代码中的 Luna 命名可暂存为迁移债，但用户面对 UI 和新规格验收必须使用 Vitora。 |
| IAC-AUTH-005 | Cycle 首页视觉、布局、图表、Tip 和 Tab 避让验收必须同时读取 `cycle-page/qa-checklist.md`。 |
| IAC-AUTH-006 | Vitora Tab assistant surface、Quick Context、Input Dock 和周期展开态验收必须同时读取 `vitora-page/qa-checklist.md`。 |
| IAC-AUTH-007 | Today / Vitora / Cycle 首层动态背景、周期色变化和 Reduce Motion 兜底验收必须同时读取 `dynamic-aura-background/qa-checklist.md`。 |

## 1. 严重级别

| Level | Meaning | Release Rule |
| --- | --- | --- |
| Blocker | 破坏核心学习循环、隐私合规、主导航或保存闭环。 | 不得继续下一阶段。 |
| Major | 路径可完成，但用户心智、反馈、退出或可访问性明显不足。 | P0 前必须修复。 |
| Minor | 不阻塞，但影响高级感、一致性或精致度。 | 记录并排期。 |

## 2. 全局验收

| ID | Rule | Pass | Fail |
| --- | --- | --- | --- |
| IAC-G-001 | 主导航固定为 Today / Vitora / Cycle。 | 三项稳定；D-087 后底部是白色一体式凹槽 Dock，`今日 / 周期` 在左右两侧，中心凹槽悬浮圆形 `+` 记录键；D-088 后 Dock 更靠底镶嵌且中心 `+` 是蓝绿色半透明霜态玻璃；D-090 后 Dock 尺寸更窄、更薄，中心 `+` 更透明，短按打开手动记录、长按打开语音记录；Today / Cycle 不显示输入条，AI 管家显示输入条；sheet 展示时全局 Dock 不透出。 | 出现第 4 tab；中央 `+` 遮挡输入 dock；Tab 图标或记录键混入输入胶囊；仍保留右侧独立圆形 `+`；中心 `+` 仍是高饱和蓝紫实心球或过实不透明；短按/长按进入同一路径；非 AI 页仍显示输入条；sheet 背后仍可见或可点全局 Dock。 |
| IAC-G-002 | 用户面对只叫 Vitora。 | 文案、tab、assistant、sheet title 均为 Vitora。 | 出现用户面对 Luna。 |
| IAC-G-003 | 3 秒可理解 Today。 | 首屏看到状态、身体要素、建议。 | 首屏是大 dashboard、空白、纯装饰或任务列表。 |
| IAC-G-004 | 无压力化。 | 无红点、连续天数、完成率、失败惩罚。 | 任何主路径出现打卡压力。 |
| IAC-G-005 | Aura Glass Pixel Companion 视觉门禁。 | 首层有弥散渐变、清透玻璃、Pixel Vitora。 | 退回 Apple Health-like flat white dashboard。 |
| IAC-G-009 | Dynamic Aura 背景统一。 | Today / Vitora / Cycle 首层背景读取同一周期色状态，动效不抢内容；D-077 后 Today 左上粉橘和右下蓝绿 Aura 更明显，底部保持珍珠白 + 蓝绿霜态玻璃而非灰底，顶部清晰、中部凝态玻璃、底部 frosted glass，Reduce Transparency 下有稳定乳白/暖雾兜底。 | 每页单独背景、教程录屏残留、背景遮挡信息、Today 拉到底变灰、渐变过淡或 Reduce Transparency 下文字不可读。 |
| IAC-G-006 | AI 可控。 | Vitora 理解结果可确认、修改、不保存。 | AI 直接写入健康事实。 |
| IAC-G-007 | Sheet 生命周期完整。 | 打开、输入、确认、保存、关闭、回来源都明确。 | 打开后无退出或保存后无反馈。 |
| IAC-G-008 | 可访问性。 | 44pt 触控、VoiceOver label、动态字体、降低动效/透明可用。 | 小目标、文字溢出、只靠颜色或动效表达。 |

## 3. Today 验收

| ID | Rule | Pass | Fail |
| --- | --- | --- | --- |
| IAC-T-001 | Today 首页结构。 | D-091 后只有低代码日期标题、小型创意趋势卡和共享聊天记录卡；趋势卡显示 `68/100`、`黄体期 D18 · 深圳`、点阵背景、粉色趋势线和温馨留言；展开态聊天记录与折叠态一致，并带细线能量进度。 | 顶部黑色三格 `TopModeSelector`、右侧黑色 `X`、粉色日历小卡、花朵玩法主视觉、水桶、种子苗、右侧维度胶囊、旧三环 `今日能量` 卡、蛋下方独立周期条、首页实时预测图、花园手册、成长册入口、过多 dashboard 卡、任务清单、花园 Tab。 |
| IAC-T-002 | 状态卡。 | 首页主视觉为 `FlowerEnergyBloomView`：橙色半碗能量弧、中心花茎和花朵、左右直线虚线轨道、前天/昨天/今天节点；当前默认 `68%` 只显示在能量碗内，不再对应花下独立 `68/100`；今天节点未达到 `80%` 阈值时显示种子苗和 `达到 80% 时将成长为向日葵`，不显示成熟向日葵；达到 `80%` 后才切换为向日葵，`100% 同品种双朵共生` 只在已开花且满足同品种条件时显示；点击有一次性轻微生长/发光反馈，无整体呼吸缩放；D-080 后花朵主视觉是接近整屏宽的横向 hero band，右侧维度胶囊独立悬浮但不覆盖水桶、`68%`、今天节点或红色虚线；D-081 后历史节点、水桶、阈值提示和右侧胶囊必须有可见间距，昨天节点不能贴进水桶左沿，读数不能顶住今天标签；D-083 后折叠/聊天态不显示维度 chips；D-086 后折叠/聊天态改为直接浮在现有 Aura 背景上的全屏前景聊天卡，不显示外层磨砂玻璃底板，旧顶部小日历和微缩花盆在完全折叠时淡出，卡片头部展示日期与 `Today` 像素标题，聊天框顶部用轻量原生像素小图表达当前时间、深圳区域和周期背景，消息区不得显示左侧虚线时间轴或时间胶囊，Vitora 消息使用柔白 Pixel 头像 + 气泡，用户消息右对齐；右侧维度切换仍能同步下方 `TodayInsightPanel`。 | 主视觉仍是旧蛋；当前 68% 被画成 80% 或开花态；花、节点、读数、气泡堆叠；昨天节点贴入水桶上沿；阈值提示、种子苗、今天标签和读数挤在一起；折叠态仍显示 `今日 / 睡眠 / 经期 / 营养` chips、compact Pixel Egg、裸聊天气泡、左侧时间轴、时间胶囊、额外灰白页面底板、外层磨砂玻璃底板或常驻输入框；右侧“今日”胶囊压在水桶或读数上；花组件使用外部图片；花变成厚重拟物或复杂插画；出现收割/花粉/图鉴/种子兑换/连续打卡入口；点击花打开分析 sheet；右侧维度切换失效。 |
| IAC-T-002A | 折叠态前景卡与日历入口。 | D-086 后白色聊天卡左右接近屏幕可用宽度，底部到 Dock 上方或略进入 Dock 后方；日期和 `Today` 靠左上，顶部留白收紧；右上汉堡按钮点击后打开左侧周期日历抽屉。 | 白卡仍像短浮卡悬在中间；标题下沉或过度居中；汉堡只是静态图标或点击无反馈；点击汉堡打开 sheet、跳转 Tab 或新增第二套日历。 |
| IAC-T-002B | 低代码标题与细进度条。 | D-089 后折叠态 `Today` 使用原生像素字，顶部信息条压缩为细线型进度条，显示时间、深圳、黄体期 D18 和 `68/100`；聊天文案为创意日温馨留言。 | 仍使用普通大号系统 `Today`；信息条保持高卡片；文案继续是高强度任务指令；出现任务、打卡或完成率心智。 |
| IAC-T-002C | 展开/折叠聊天一致性。 | D-091 后展开态和折叠态显示同一套聊天记录、同一条细线能量进度和同一组创意日 Vitora 回复；折叠态白卡底部延伸到 Dock 后方，不留明显空隙。 | 展开态仍显示旧三环 `今日能量` 卡；展开态与折叠态文案不一致；折叠态白卡底部停在 Dock 上方形成明显断层。 |
| IAC-T-003 | 记录/校准入口。 | 对象长按或全局圆形 `+` 打开 3/4 contextual sheet。 | 首页显示校准 chips；跳到 Vitora tab 后用户丢失上下文。 |
| IAC-T-004 | 身体要素。 | 每项有数值、趋势、对今天意义，可二层查看。 | 堆指标但不解释为什么。 |
| IAC-T-005 | TodayInsightPanel。 | 首页下方信息框随主题切换；`今日能量` 默认有三枚环形指标、chips、Vitora 黄色提示、黑色 `+ 记一笔` 和 `今日 68/100`；点击 `为什么` 或标题后展开原因、轻建议和 `✓ 提醒`；睡眠展示睡眠详情，经期展示黄体期 D18 解释，营养展示补水/补给记录并标注只记录已在使用内容。 | 继续显示旧扑克牌式 `智能监测` 首页主卡；只是 toast 或泛建议；`+ 记一笔` 切换主 Tab；营养卡出现购买引导；出现打卡、完成率、任务失败或红点压力。 |
| IAC-T-006 | Today 日历入口。 | 顶部 38-44pt 小日历 icon 进入左侧 3/4 文具周期日历浮层并返回 Today；只有 icon 本体 44pt 可点，周围空白不可触发；浮层顶部无“周期日历”标题和说明副标题，包含周期图例、英文周标题、阶段色月历、浅粉便签和木制记忆箱；抽屉打开和关闭动画期间后方 Today/Dock 不可点。 | 日历放到 Cycle 首页作为主卡；入口显示睡眠/步数/长卡片能量条；点击 icon 周围空白也打开日历；浮层关闭时点击透传到日历入口；浮层保留旧标题、副标题、行动链、过去几天、底部双按钮或普通彩色记忆箱。 |
| IAC-T-007 | 花组件 / 今日分析 / 快捷记录。 | 首屏无顶部下拉 Energy Ball；花组件和右侧胶囊维度切换压缩在首屏上方约 1/3，红色虚线避开聊天/反馈条；花下方不再有独立能量数字或 `查看分析` CTA，今日分析由 `TodayInsightPanel` 标题 / `为什么` 等入口承接；快捷记录完成后 `today.record.feedback` 展示记录摘要和生活方式温馨提示；无记录时展示当前 Vitora 提示；反馈条右侧不再展示灰色圆形附加记录/提醒按钮。 | 仍显示“轻轻下拉”提示；花区向下侵占反馈区；红色虚线挡住聊天框；花下仍显示 `68/100 查看分析`；反馈条右侧出现 `记录饮食`、`补水提醒`、`查看记录` 或 `继续补充` 等灰色圆形按钮；首页仍有三模式切换、实时预测图或校准 chips；关闭快捷记录时误触打开日历；今日分析缺少关键依据或今日推荐。 |
| IAC-R-001 | 快捷记录双入口。 | Dock 中心凹槽圆形 `+` 和 Today 统一轨道 `+记录` 打开同一个 `VitoraContextualSheet`；Dock `+` 点按默认进入 `手动记账`，长按进入 `AI记录 > 语音`，辅助操作可直接触发 `语音记录 / 打字记录`；来源显示为 `快捷记录` 或当前展开主题的 `今日能量记录 / 睡眠记录 / 经期记录 / 营养记录`；关闭后回到来源页，不切主 Tab，sheet 展示时全局 Dock 隐藏。 | 两个入口打开不同记录页；轨道 `+记录` 改变主题展开状态；打开后切到 Vitora Tab；sheet 背后 Dock 可见或可点；右侧仍残留独立圆形 `+`；长按 `+` 恢复首页常驻输入框或发送按钮。 |
| IAC-R-002 | 手动记账式快捷记录。 | 默认 `手动记账` active；顶部有 44pt 关闭按钮并支持下拉关闭；手动页显示 `是经期 / 否经期` 状态切换；一次展示 5-8 个健康大纲，每个大纲默认露出 5 个高频词条；点击大纲或 `更多` 展开完整支线，点击词条直接选中/取消；`是经期` 包含经期情况、身体状态、心情、服药、营养补充剂、健康小忌，`否经期` 包含白带、身体状态、心情、皮肤、爱爱、营养补充剂、健康小忌；日期/周期上下文字段保留；底部记录卡只展示图片、拍照、语音三个入口；下方有 `vitora.record.note.input` 留言条；`完成` 不会卡在不可点击状态，完成后关闭 sheet 并写入运行态 `RecentRecordFeedback`。 | 继续显示旧 `日常 / 经期` 或 `症状 / 情绪 / 月经 / 营养` sub-tab；继续显示 `支出 / 收入 / 转账`、餐饮财务分类、金额输入卡、金额数字键盘、删除键或金额提交依赖；大纲不能展开；更多没有展开；完成进入手动页不可点；关闭或点空白透传打开后方日历。 |
| IAC-R-003 | AI 快捷记录。 | `AI记录` 展示 AI 标志，并提供 `语音 / 打字` 二级切换；`打字` 展示自然语言输入、发送按钮、快捷示例、结构化字段卡和留言条；`语音` 展示大号 mic、录音状态、实时转写展示、`重新说 / 转打字 / 解析`，权限拒绝或不可用时提示并降级到打字；输入 `今天有点头痛，吃了 B6 和镁` 后展示 `AI 已拆出字段`，字段为健康类别/项目/细节；完成后关闭 sheet 并在 Today 反馈条展示记录摘要和生活方式提示；不生成诊断、不接真实模型、不新增 schema、不记录 raw input、原始语音、转写原文、prompt 或完整 AI 输出。 | 出现真实模型调用、诊断、购买引导、黑盒解析、Luna 用户文案、财务金额字段或把原文/语音转写写入日志；语音不可用时崩溃；AI 打字入口回到首页常驻输入框。 |
| IAC-T-008 | 低数据。 | Today 仍可用，说明“基于目前信息”，可告诉 Vitora 或管理数据来源。 | 空白、失败感、要求授权后才能继续。 |

## 4. Vitora 验收

| ID | Rule | Pass | Fail |
| --- | --- | --- | --- |
| IAC-V-001 | Vitora Tab 是 AI-native assistant surface。 | Pixel Vitora、Vitora 知道、对话、直接问、快捷上下文、input dock 同屏；具体执行以 `vitora-page/vitora-page-spec.md` 为准。 | 空白聊天页；普通功能宫格；dashboard 堆卡。 |
| IAC-V-002 | Pixel Vitora IP。 | 继承 `pixel-vitora-ip/pixel-vitora-ip-spec.md` 的玻璃像素小球 baseline；眼睛像素化，呼吸/眨眼轻微。 | smooth orb、人类头像、宠物、普通 icon；每页单独发明不同 IP。 |
| IAC-V-003 | 直接问。 | 薄横向玻璃条，帮助 cold start。 | 大按钮宫格或营销文案。 |
| IAC-V-004 | 快捷上下文。 | 小 icon chips 位于日期行下方，带入上下文；`周期 / 睡眠 / 营养` 都使用统一上下文摘要卡，右上角可取消。 | 大卡片占满屏幕；和输入断裂；周期使用专属报表而睡眠/营养只是简短卡；把报表做成 Cycle 首页。 |
| IAC-V-005 | 输入 dock。 | Dock 由 bottom safe area 承载；D-087 后下层为一体式白色凹槽 Dock，左右是 `今日 / 周期`，中心悬浮圆形 `+`；D-088 后 Dock 不再悬浮过高，中心 `+` 为更透明蓝绿色霜态玻璃；D-090 后 Dock 和左右 Tab 胶囊更紧凑，中心 `+` 短按只打开手动快捷补充 sheet、长按进入 AI 语音记录；AI 管家页上层顺序固定为语音/键盘、输入或内联语音条、发送；输入条内无 `+`；右侧发送固定且状态清楚；点击 `AI管家` 只切换页面并展示输入栏，点击输入栏本身才聚焦键盘；sheet 打开时全局 Dock 隐藏；键盘和 tab 不遮挡。 | 无发送路径；中央 `+` 遮挡输入；记录键进入输入胶囊；点击记录切到 Vitora Tab；短按/长按进入同一路径；仍出现旧居中胶囊 + 右侧独立 `+`；中心 `+` 仍是蓝紫实心球或过实不透明；非 AI 页显示输入条；sheet 与背景 Dock 叠加；语音状态无退出；语音态文字竖排或按钮挤压。 |
| IAC-V-006 | 聊天聚焦态。 | 点击 `AI管家` 后先保持实体 hero 卡片态；用户上拉后隐藏 hero 卡，完整展示日期行、topic rail、说明、消息、能力反馈和输入 dock，不切回 Today。 | 点击进入即大展开；上拉后跳回 Today；顶部仍占半屏；聊天内容被底部 Dock 遮挡。 |
| IAC-V-007 | Rich response。 | Vitora 先展示理解、影响范围、建议更新，用户确认后保存。 | 输入发送后直接消失或直接写入。 |
| IAC-V-008 | Contextual sheet 升级。 | 3/4 sheet 可上滑进入完整 context mode。 | 从来源唤醒后直接切 tab 或打开完全不同页面。 |

## 5. Cycle 验收

| ID | Rule | Pass | Fail |
| --- | --- | --- | --- |
| IAC-CY-001 | Cycle 首页只承担长期节律。 | 左上我的入口 + Header 胶囊 `本周 / 趋势对比 / 近期` + 右上周期卡片分享 + 统一地图报告框；框内上方为 `FlowerMapView`，下方为单张详情内容区；首页不显示可见 `周期回顾` 标题、能量动态卡、三张节律洞察卡或 `本周期花架证据`；具体执行以 `cycle-page/cycle-page-spec.md` 为准。 | 周期日历、30 天成长册、花园手册、种子选择、任务化花田地图、能量动态下方卡或花架证据作为首页主卡；地图和详情被拆成两个明显独立 section；重复 Today 当前状态；出现任务、打卡、完成率或连续天数。 |
| IAC-CY-001A | 花之地图。 | `cycle.flowerMap` 使用原生 SwiftUI 绘制深圳不规则菱形 tile 地图，顶部高度约首屏 2/3 到 3/4；透明圆形 `种下今天`、空地点击、已种花详情、问号说明和城市路线节点均可点击；种花后显示 `今日已种下`，详情报告框直接在地图下方。 | 使用 RN/Expo、图片素材、地图 SVG、城市建筑 icon 或复杂动画库；地图占满整页导致报告卡不可达；按钮像打卡任务或完成率。 |
| IAC-CY-002 | 周期回顾理解。 | Header 胶囊用 `本周 / 趋势对比 / 近期` 表达三页报告；点击不同胶囊后地图下方同一张详情框内容和 accent 同步切换；本周展示柱状复盘、三段说明、监测摘要和周期进度，趋势展示月度对比和归因总结，近期展示折线观察、监测解释和下一步。 | 只显示 Day 数字，没有说明和 Today 的关系；仍是旧四 tab `经历 / 监测`；地图下方仍出现第二套文件夹 tab；多张高饱和深色卡造成阅读疲劳；花园/成长卡像日历或任务完成度。 |
| IAC-CY-003 | 趋势报告。 | `趋势（对比）` tab 展示月度综合对比、4 行蓝色进度条、归因解释和本月总结；`近期` tab 展示 6/1-6/7 能量折线和下一步。 | 堆高级医疗图表、商业化入口、订阅锁或与 Today 当前状态重复。 |
| IAC-CY-004 | 阶段二层。 | 透明解释：如何判断、预测窗口、对今天意义、校准。 | 二层和首页信息重复，没有新价值。 |
| IAC-CY-005 | 能量二层。 | 趋势探索 + 图层 + 关键点 + callout。 | 只有一张放大的同款曲线。 |
| IAC-CY-006 | 顶部入口。 | 左上头像进入 P0 设置/我的；右上分享只分享当前周期卡片截图。 | 支撑入口藏太深、出现宽设置中心、分享导出记录原文或隐藏健康数据。 |

## 6. Askable Surface 验收

| ID | Rule | Pass | Fail |
| --- | --- | --- | --- |
| IAC-AS-001 | 单击是主路径。 | 卡片单击进入二层 IA。 | 单击只弹 callout 或无反应。 |
| IAC-AS-002 | 长按是 shortcut。 | 长按打开 iOS context menu：问 Vitora / 告诉不准 / 查看详情。 | 长按是唯一入口；菜单项超过 3 个且无关。 |
| IAC-AS-003 | Callout 是局部解释。 | 点图表点显示小气泡，可进入问 Vitora。 | Callout 替代二层详情。 |
| IAC-AS-004 | TipKit 教学。 | 第一次停留 2-3 秒后轻提示一次，成功使用后消失。 | 每次弹提示或完全无可发现性。 |
| IAC-AS-005 | 二层 fallback。 | 二层有 `⋯` 或小 Vitora 图标打开同样 actions。 | 不会长按的用户无法访问 Vitora 动作。 |

## 7. Support / Trust 验收

| ID | Rule | Pass | Fail |
| --- | --- | --- | --- |
| IAC-S-001 | 支撑项边界。 | 只显示个人资料、HealthKit/数据来源、营养补给、提醒偏好、数据导出、隐私法律与账号移除。 | VIP、主题、小组件、帮助墙、养成进度进入 P0。 |
| IAC-S-002 | 隐私合规。 | 导出/移除有确认、进度、完成反馈；日志无健康数值和记录原文。 | 危险动作无确认；日志泄露敏感内容。 |
| IAC-S-003 | 营养补给。 | 管理页在支撑区；快捷新增可从 Vitora context 发生。 | 营养入口商业化或强销售。 |
| IAC-S-004 | 提醒偏好。 | 只服务今日建议和晚间复盘；权限拒绝不影响意图。 | 通知成为使用门槛。 |

## 8. QA 场景

| ID | Scenario | Expected |
| --- | --- | --- |
| IAC-QA-001 | 新装打开，完成注册页和聊天式补充信息，跳过 HealthKit / 设备绑定。 | 注册按钮可点；登录后不再询问昵称；第二页用 Pixel Vitora 聊天问答合并关注方向、恢复方式、周期清晰度和设备绑定；点击 `进入 Vitora` 后直接进入低数据 Today。 |
| IAC-QA-002 | Today 首屏丰富数据。 | 综合能量碗、单层高对比数字、数字下方状态词、碗底三阶段周期圆弧、身体翻译和组合建议 3 秒内可读；首页没有数据小标签、实时预测图和校准 chips。 |
| IAC-QA-003 | Today Energy Bowl。 | 点击能量碗进入状态详情；点击 `查看数据` 进入今日分析；数据小标签、综合实时预测当前时间气泡、横向身体要素卡可见。 |
| IAC-QA-004 | 点击 Today 状态卡。 | 进入状态详情；返回 Today。 |
| IAC-QA-005 | 长按 Today 状态卡。 | 出 context menu；选择问 Vitora 打开 3/4 sheet。 |
| IAC-QA-006 | 点击全局圆形 `+` 或对象长按校准。 | Contextual sheet 带来源上下文；确认后更新理解，不切换主 Tab。 |
| IAC-QA-007 | 进入 Vitora Tab。 | Pixel Vitora hero、Vitora 知道、直接问、chips、input dock 可见。 |
| IAC-QA-008 | Vitora 输入文字。 | Rich response card 可确认、修改、不保存。 |
| IAC-QA-008A | 全局快捷记录。 | 点击 Dock 中心凹槽圆形 `+` 或 Today 明确记录入口打开 Vitora contextual sheet；默认 `手动记账` active，看到 `是经期 / 否经期`、健康大纲、默认 5 个词条、日期/周期上下文字段、图片/拍照/语音记录卡、留言条和可点击 `完成`；点击一个大纲或 `更多` 展开支线；关闭按钮、点空白和下拉关闭都只关闭 sheet，不触发日历或切 Tab；Dock 入口来源为 `快捷记录`，Today 入口在主题展开时来源带当前主题；不切换到 Vitora Tab，且不改变当前主题展开状态。 |
| IAC-QA-008B | AI 快捷记录。 | 长按 Dock 中心凹槽圆形 `+` 打开 Vitora contextual sheet，默认 `AI记录` active 且 `语音` selected；可看到 mic、录音状态和 `重新说 / 转打字 / 解析`；切到 `打字` 后输入 `今天有点头痛，吃了 B6 和镁` 并发送，展示 `AI 已拆出字段`、健康类别、项目和细节；留言条存在；语音权限拒绝时降级到打字且不崩溃；完成后关闭并在 Today `today.record.feedback` 展示记录摘要和生活方式提示。 |
| IAC-QA-009 | Vitora 语音入口。 | 录音态、重录、转写并理解路径可见。 |
| IAC-QA-010 | Cycle 首页。 | 顶部 Header 胶囊为 `本周 / 趋势对比 / 近期`；`cycle.mapReport.frame` 同时包住 `cycle.flowerMap` 和下方详情内容；地图可种花/看详情/打开玩法说明；地图下方单张详情内容区可见且无第二套文件夹 tab；默认本周，点击趋势对比和近期后内容正确切换；没有日历首页、能量动态下方卡、节律洞察卡或花架证据。 |
| IAC-QA-011 | Cycle 能量二层点曲线点。 | 显示 callout；可问 Vitora。 |
| IAC-QA-012 | Today 顶部日历。 | 打开周期日历并返回 Today。 |
| IAC-QA-012A | Today 日历选择不同日期。 | 返回 Today 后背景按周期阶段 crossfade 变化，Reduce Motion 下显示 poster。 |
| IAC-QA-013 | 晚间复盘。 | 显示早上状态和晚间反馈，不问“完成了吗”。 |
| IAC-QA-014 | 设置入口。 | 只显示 P0 支撑项。 |
| IAC-QA-015 | Accessibility。 | 动态字体、VoiceOver、Reduce Motion/Transparency 通过。 |

## 9. 反向验收：出现即失败

| ID | Fail Pattern |
| --- | --- |
| IAC-N-001 | 用户面对 UI 出现 Luna。 |
| IAC-N-002 | Today 有独立常驻 `+记录` section。 |
| IAC-N-003 | Cycle 首页是日历。 |
| IAC-N-004 | Vitora Tab 是空白聊天或功能 dashboard。 |
| IAC-N-005 | Today 首屏出现顶部下拉 Energy Ball 提示或常驻大能量球压住核心信息。 |
| IAC-N-006 | Pixel Vitora 被 smooth orb、人类头像、宠物或普通 icon 替代。 |
| IAC-N-007 | Apple Health dashboard-like flat white visual 成为首层方向。 |
| IAC-N-008 | 长按是访问 Vitora 的唯一方式。 |
| IAC-N-009 | AI 输出未经用户确认直接保存为健康事实。 |
| IAC-N-010 | `/speckit.implement` 继续旧 T102 Evening Review 而不先做 pivot adaptation。 |
